{
  "name": "quantum-assistant",
  "version": "0.1.0",
  "private": true,
  "description": "Asistente general de escritorio con orbe, vision de pantalla y Gemini Live",
  "license": "UNLICENSED",
  "packageManager": "pnpm@9.15.4",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test",
    "dev": "pnpm --filter @quantumhive/assistant-desktop dev",
    "dist:win": "pnpm --filter @quantumhive/assistant-desktop dist:win"
  },
  "devDependencies": {
    "typescript": "^5.7.3"
  }
}
